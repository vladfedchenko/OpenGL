/*
 * Camera.h
 *
 *  Created on: Aug 21, 2016
 *      Author: vladfedchenko
 */

#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>

namespace VladFedchenko{
namespace GL{

	class Camera
	{
	private:
		glm::vec3 eye;
		glm::vec3 center;
		glm::vec3 up;

		glm::mat4 lookAtMatr;
		glm::mat4 projectionMatr;

	public:
		Camera(glm::vec3 eye, glm::vec3 center, glm::vec3 up, float fov, float aspectRation, float nearPlane, float farPlane);
		~Camera();

		void ResetProjection(float fov, float aspectRation, float nearPlane, float farPlane);
		void ResetView(glm::vec3 eye, glm::vec3 center, glm::vec3 up);
	};

}}
